import { Button } from "@mui/material";

const Base = () => {
  return (
    <div class="base">
      <Button variant="outlined">Outlined</Button>
      <span class="clt_profile-banner-name">我的諮詢</span>
    </div>
  );
};

export default Base;
